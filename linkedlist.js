const LinkedList = () => {
  const append = (value) => {

  };

  const prepend = (value) => {

  };

  const size = () => {

  };

  const head = () => {

  };

  const tail = () => {

  };

  const at = (index) => {

  };

  const pop = () => {

  };

  const contains = (value) => {

  };

  const find = (value) => {

  };

  const toString = () => {

  };

  const insertAt = (value, index) => {

  };

  const removeAt = (index) => {

  };

  return {
    append,
    prepend,
    size,
    head,
    tail,
    at,
    pop,
    contains,
    find,
    toString,
    insertAt,
    removeAt,
  };
};

const Node = () => ({
  value: null,
  nextNode: null,
});
